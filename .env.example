VITE_ACCESS_API_BASE_URL=https://localhost:7201
VITE_DEFAULT_LOCALE=es-AR
VITE_GOOGLE_CLIENT_ID=644236758922-hbf6kbllem8ljmtamrhbjp1pp8r8m4ap.apps.googleusercontent.com

# ── Chromatic ──────────────────────────────────────────────────────────────
# Token del proyecto de Chromatic. NO se versiona: hasta 2026-08-07 vivía en
# claro dentro de `package.json` (script `chromatic`), o sea que estuvo
# publicado en el historial de git.
#
# ⚠️ MOVERLO A UNA VARIABLE NO LO DESPUBLICA. Sacarlo del `package.json` evita
# que se filtre de nuevo, pero el valor viejo sigue en el historial del repo y
# hay que ROTARLO en Chromatic. Es el `PENDIENTE (DA-FE-06)` de
# `06-capa-0-frontend.md` §6: "mover a secret de CI Y ROTAR". Decide el PO.
#
# En CI se inyecta como secret del repositorio (`CHROMATIC_PROJECT_TOKEN`).
CHROMATIC_PROJECT_TOKEN=
