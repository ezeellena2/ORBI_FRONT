# Bitácora — slice F-03 · Enchufe de módulo

> Escrita a mano, con hora real. **No** se usa `orbi-bitacora.ps1 front`: crea
> `TracAutoV2/src/front/docs/…`, un árbol que simula un microservicio .NET inexistente (pasó en
> F-02 y se limpió).

| Hora | Paso | Evento | Detalle |
|---|---|---|---|
| 09:41 | f-00 | INICIO | Grafo real de dependencias antes de tocar nada. Baseline medido: **453 tests / 33 archivos**, `eslint-suppressions.json` con **13 archivos** (25 counts), **0 tests** en `src/app` y `src/features`. |
| 09:44 | f-00 | HALLAZGO | El grep por RUTA (`module-nav/registro`) **pierde a `ColumnaDeModulo.tsx`**, que importa relativo (`./registro`). Es el mismo modo de falla de F-02: inventariar por string en vez de por símbolo. Rehecho el grafo por SÍMBOLO exportado. |
| 09:46 | f-00 | CONFIRMADO | Con el grafo por símbolo, el inventario del prompt **se confirma**: 3 consumidores de `registro.ts` (`useBreadcrumb`, `ColumnaDeModulo`, `nav-config`) y 4 de `nav-config.ts` (`SidebarNav`, `SidebarNavItem`, `SidebarNavLabel`, `useBreadcrumb`). Ningún archivo sorpresa **por import**. |
| 09:48 | f-00 | HALLAZGO | **El `vocabulario-mapa.ts` de este slice: `features/shell/components/dashboard/DashboardOnboarding.tsx`.** Conoce a Flota y **no importa nada** de `registro.ts`: hardcodea `'/app/flota/vehiculos/onboarding'` y `modulos.includes('flota')`. Ni el grep por ruta ni el grep por símbolo lo encuentran. Su propio docblock declara el pendiente del PO. |
| 09:52 | f-00 | HALLAZGO | **`nav-config.ts:65` nombra `'flota'` a mano** (`linkDeModulo('flota')`). Agregar un módulo obliga a editar un archivo del shell aunque exista el manifiesto: `NAV_CONFIG` es un array ordenado escrito a mano que intercala labels, Dashboard y Usuarios con los links de módulo. |
| 09:55 | f-00 | HALLAZGO | **i18n: `src/shared/i18n/index.ts` hardcodea `ns: ['common','flota']`** con imports estáticos de `flota.json`, y las 8 claves `shell.nav.flota*` viven en `common.json` (×2 idiomas). Agregar un módulo hoy toca **6 archivos compartidos en 3 capas**, no 3. |
| 09:57 | f-00 | CONFIRMADO | **No hay conflicto lint/mecanismo**: `eslint.config.js` no tiene bloque para `src/app/**`, así que `app/registry/` puede importar manifiestos (y `AppRouter:27` ya importa `@/modules/flota/FlotaRoutes`). El bloque de `src/shared/**` **sí** prohíbe `@/modules/*` y `@/app/*` — por eso la registración de namespaces i18n **no puede** vivir en `shared/i18n/`. |
| 09:58 | f-00 | PARADA | Inventario cerrado y 2 decisiones abiertas para el PO (i18n y alcance de `routes` en el manifiesto). Esperando visto bueno antes de f-01. |
| 10:02 | f-00 | CONFIRMADO | **Decisión del PO (i18n): opción (a)** — el módulo es dueño de sus etiquetas (`flota.json` → `nav.*`, `labelKey: 'flota:nav.x'`) y **`app/` registra el namespace** con `addResourceBundle` al arrancar, porque F9 le prohíbe a `shared/` importar `modules/`. En `common.json` queda solo el vocabulario compartido. |
| 10:02 | f-00 | CONFIRMADO | **Decisión del PO (alcance): nav + rutas.** El manifiesto lleva `routes` y `basePath` como manda el contrato §3; `AppRouter` deja de hardcodear el `<Route>` por módulo. Sin eso el criterio 3 no cierra. |
| 10:04 | f-01 | INICIO | Test del registro **contra el código actual**, antes de refactorizar. Hoy hay 0 tests de shell: la red va primero. |
| 11:38 | f-01 | CORRECCION | El parser de rutas del test se rompía: `replace(/\/\*[\s\S]*?\*\//g,'')` trataba el `flota/*` **citado dentro de un docblock** como apertura de comentario y se comía código real (parseaba `"flota}\r\n <Route path="` como ruta). Reescrito línea a línea con máquina de estados + red de seguridad que tira si una ruta trae basura. |
| 11:40 | f-01 | HALLAZGO | **`rutasHijas` tiene DOS significados y el nombre solo describe uno.** `['/app/flota/problemas/', '/app/flota/integraciones']`: la primera es prefijo de hijas (con barra), la segunda es **alias de una ruta hermana** (exacta, sin barra) heredado de `aliases` del mockup. Un test que exigía barra final en todas se puso rojo contra el código correcto: el invariante estaba mal formulado, no el dato. |
| 11:42 | f-01 | CONFIRMADO | Mutación verificada: montar `geozonas` en `FlotaRoutes` (que `AppRouter` sirve como placeholder estático) pone **rojo solo** el test de la trampa del splat. Revertido por edición, `git diff` vacío. |
| 11:43 | f-01 | CONFIRMADO | Segunda mutación: `rutasHijas` = la ruta propia sin barra → **rojo solo** en el test del dato real. La primera versión de ese test NO la mataba (usaba un ítem armado a mano): hueco tapado. |
| 11:45 | f-01 | PUERTA-VERDE | **15 tests nuevos** contra el código ACTUAL, antes de refactorizar. Puerta: TC=0 LINT=0 TEST=468/34 archivos (baseline 453/33). |
| 11:47 | f-02 | HALLAZGO | **El contrato §3 se contradice con el lint del repo.** Ubica los tipos del manifiesto en `@/features/shell/module-nav/tipos`, y un módulo que importe de ahí es **error de lint** (F9: *«un módulo consume shared/, services/ y stores/, nunca features/»*). El ejemplo del propio contrato no compila en este repo. Los tipos se mudaron a `src/shared/types/modulo.ts` —lo que el prompt del slice ya decía—. Gana el lint, que es el que corre. **Drift a reportar en `08-enchufe-de-modulo.md` §3.** |
| 11:49 | f-02 | CONFIRMADO | Manifiesto de Flota escrito (`modulo.manifest.ts`): navegación con las etiquetas ya en su namespace (`flota:nav.*`), `routes: FlotaRoutes`, `basePath: 'flota'`, `i18n` con sus dos catálogos y `orden: 10`. Los comentarios del registro viejo se conservaron literales: explican por qué Vehículos va primero y por qué `grupoOperacion` se queda en `common`. |
| 11:49 | f-03 | CONFIRMADO | Agregador `app/registry/` + `registro.ts` sin la constante `FLOTA` + `nav-config` sin `linkDeModulo('flota')` + `AppRouter` sin el `<Route path="flota/*">` hardcodeado. **La API de `registro.ts` no cambió**: sus 3 consumidores no se enteraron. Cambió de dónde salen los datos, no cómo se leen. |
| 11:50 | f-03 | HALLAZGO | **Este slice INTRODUCE una trampa que antes no existía.** Con el registro escrito a mano el orden del array era el orden del sidebar; con N manifiestos, `moduloDeLaRuta` —que devuelve *el primero que matchea*— pasaría a depender del **orden de importación**. Cerrado con `orden` explícito + desempate por `key`, y un verificador que **tira al arrancar** si dos módulos declaran la misma ruta, nombrando a los dos. |
| 11:51 | f-03 | CORRECCION | `ReferenceError: document is not defined` al importar `@/app/registry` desde un test de Node: arrastraba `@/shared/i18n`, cuyo `init()` de nivel superior toca `document`. Vitest avisó que eso *«might cause false positive tests»*. Partido en dos: `index.ts` es **dato puro** y la registración de namespaces vive en `app/registry/i18n.ts`, que llama `main.tsx`. Importar el registro tiene que ser gratis. |
| 11:52 | f-03 | CONFIRMADO | 8 claves `shell.nav.flota*` movidas de `common.json` a `flota.json` como `nav.*`, en los dos idiomas, **por inserción textual respetando el CRLF** — no con `json.dumps`, que en F-02 reformateó el archivo entero para agregar 6 claves. Paridad verificada: `common 531 · flota 1544`. |
| 11:53 | f-04 | CONFIRMADO | **Configuración se queda en el registro central, y no es pereza.** No vive en `src/modules/`, no se contrata, y el shell la tiene con cero módulos instalados. Además **no cumple el contrato del manifiesto ni podría**: §3 exige `basePath` + `routes: ComponentType`, y sus 8 secciones son placeholders sueltos en `AppRouter`, no un splat delegado. Darle manifiesto obligaría a fabricar un `ConfiguracionRoutes` inexistente. Es la entrada del SHELL en el registro. |
| 11:55 | f-05 | CONFIRMADO | **Módulo de mentira enchufado** (`src/modules/prueba/`: 1 ruta, 1 ítem de nav). Aparece en el sidebar, en el registro de columnas y `moduloDeLaRuta('/app/prueba/hola')` lo resuelve. Lo único editado fuera de su carpeta: **una línea** en `app/registry/index.ts`. Cero archivos de `features/shell/`. |
| 11:56 | f-05 | HALLAZGO | **El módulo falso delató a mi propio test.** `existeClave` tenía los catálogos i18n hardcodeados (`{ common, flota }`), así que `prueba:nav.*` se puso rojo: el test conocía los módulos **por nombre**, que es exactamente lo que este slice existe para eliminar. Reescrito como `catalogosDe(idioma)` derivado de `MANIFIESTOS`. Sin el módulo falso, ese acoplamiento no se veía. |
| 11:58 | f-05 | HALLAZGO | **Con UN módulo instalado, un test de orden o de choque es vacuo**: pasa con cualquier implementación, incluso con una que no ordene ni verifique nada. Por eso `ordenarManifiestos` y `verificarQueNoSeSolapan` se exportan puras y se ejercitan con manifiestos fabricados. 11 tests nuevos en `app/registry/registry.test.ts`. |
| 12:00 | f-05 | CORRECCION | **Me comí los cambios de F-03 en `nav-config.ts`**: para limpiar una mutación usé `git checkout --`, y el archivo estaba modificado-sin-commitear, así que volvió a HEAD. Es justo lo que la regla *«revertir por edición»* previene, y la violé yo. Restaurado por ediciones ancladas, CRLF preservado (107/0), diff verificado línea por línea. **Causa raíz de la mutación fallida: el revert usaba `''` como ancla y `count('')` da 4378.** |
| 12:01 | f-05 | CONFIRMADO | 4 mutaciones, cada una **roja y revertida por edición**: no ordenar → 2 rojos · quitar el desempate por `key` → 1 · mutar la lista de entrada → 1 · el verificador de choques como no-op → 2 · el sidebar sin el spread del agregador → 1. |
| 12:03 | CIERRE | PUERTA-VERDE | TC=0 · LINT=0 (tokens y i18n OK) · TEST **479/35 archivos** (baseline 453/33; +26 tests) · BUILD=0 · STORYBOOK=0. Módulo falso borrado, `INSTALADOS` de vuelta en `[flota]`. `grep -rn "@/modules/" src/features/shell/` → **0**. `eslint-suppressions.json` → **13 archivos**, no creció. |
| 12:06 | CIERRE | CONFIRMADO | **Verificación en vivo** (`npm run dev`, sin backend): la app monta sin errores de consola y sin el invariant de react-router. Sondeado el i18n del navegador: `ns: ['common','flota']`, `hasResourceBundle('es-AR','flota') = true`, `t('flota:nav.modulo') = "Telemetría y flota"`, `exists('shell.nav.flota') = false`. Es la única prueba de que `registrarI18nDeModulos()` corre de verdad al arrancar — ningún test lo ejercita. |
